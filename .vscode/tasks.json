{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make Build",
            "type": "shell",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${fileDirname}/../build" // 重要：指定make在工作目录执行
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        },
    ]
}